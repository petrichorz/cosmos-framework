# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: OpenMDW-1.1

# Short causal continued-pretraining trial for Cosmos3-Edge.
#
# Optimizer settings follow the Cosmos 3 Generator pre-training recipe:
# FusedAdamW, lr=1e-4, betas=(0.9, 0.99), weight_decay=0.05,
# gradient clipping at 1.0, and linear warmup/decay to 0.30x peak LR.
# The paper does not disclose the exact warmup length. This 2k trial uses a
# 50-step warmup, which is a local choice rather than a paper-reported value.
# The vision_sft_edge experiment already applies 10% CFG caption dropout.

[job]
task         = "vfm"
experiment   = "vision_sft_edge_lerobot3"
project      = "cosmos3"
group        = "causal_pretrain"
name         = "vision_causal_pretrain_2k_edge"
wandb_mode   = "offline"

[model]
precision                           = "bfloat16"
joint_attn_implementation           = "teacher_forcing"
causal_training_strategy            = "teacher_forcing"
teacher_forcing_block_size_min      = 1
teacher_forcing_block_size_max      = 4
teacher_forcing_history_blocks_min  = 1
teacher_forcing_history_blocks_max  = 64
teacher_forcing_dense_mode          = "per_sample"
teacher_forcing_visualize_sdpa_mask = false

[model.ema]
enabled         = false
rate            = 0.1
iteration_shift = 0

[model.parallelism]
data_parallel_shard_degree     = -1
data_parallel_replicate_degree = 1
fsdp_mixed_precision_enabled   = true
fsdp_master_dtype              = "float32"

[model.compile]
enabled         = false
compile_dynamic = true

[model.activation_checkpointing]
mode               = "full"
save_ops_regex     = ["fmha"]
preserve_rng_state = true
determinism_check  = "default"

[model.tokenizer]
vae_path = "${oc.env:WAN_VAE_PATH}"

[optimizer]
betas          = [0.9, 0.99]
eps            = 1.0e-6
fused          = true
keys_to_select = [
    "moe_gen",
    "time_embedder",
    "vae2llm",
    "llm2vae",
    "k_norm_und_for_gen",
]
lr           = 1.0e-4
weight_decay = 0.05

[scheduler]
lr_scheduler_type = "LambdaLinear"
cycle_lengths      = [2000]
f_max              = [1.0]
f_min              = [0.3]
f_start            = [0.0]
verbosity_interval = 0
warm_up_steps      = [50]

[trainer]
distributed_parallelism = "fsdp"
grad_accum_iter         = 1
logging_iter            = 1
max_iter                = 9

[trainer.callbacks.compile_tokenizer]
compile_after_iterations = 3
enabled                  = false

[trainer.callbacks.grad_clip]
clip_norm    = 1.0
force_finite = true

[checkpoint]
keys_to_skip_loading = ["net_ema."]
load_path            = "${oc.env:BASE_CHECKPOINT_PATH}"
save_iter            = 1000

[dataloader_train]
max_sequence_length = 54000 # 45056
max_caption_tokens = 2048

min_video_frames    = 61          # episode 最少帧数
max_video_duration_s = 61.0       # stress profile: maximum clip duration in seconds
max_video_fps        = 15.0       # 超过后按整数帧间隔下采样；设为 0 可关闭

video_backend     = "pyav" # 可切换为 "pyav"
video_resize_mode = "decode_transform" # 可切换为 "decode_transform"
video_tolerance_s = 0.033       # PyAV 最近帧匹配容差；TorchCodec 后端不使用

long_video_policy = "split"
video_window_overlap_s = 2.0
